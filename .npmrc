# Publish and install this scope from the public npm registry.
# A scope registry in a user's ~/.npmrc otherwise takes precedence over
# publishConfig, which would send `npm publish` somewhere unintended.
@barissozudogru:registry=https://registry.npmjs.org
