name: test-gap-mcp
description: Analyze test coverage gaps - parse lcov/istanbul/cobertura, detect untested functions, calculate complexity
author: barissozudogru
license: MIT
repository: https://github.com/barissozudogru/test-gap-mcp
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: 'npx',
      args: ['-y', '@barissozudogru/test-gap-mcp']
    })
